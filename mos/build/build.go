package build

const (
	// ManifestFileName is the name of the file that describes the firmware
	ManifestFileName = "mos.yml"
	// ManifestArchFileFmt is the format for arch-specific manifest filename
	ManifestArchFileFmt = "mos_%s.yml"

	// FirmwareBuildDirKey is the name of the build directory
	FirmwareBuildDirKey = "build"

	// FirmwareFileName is the name of the current firmware in the build dir
	FirmwareFileName = "fw.zip"
)
