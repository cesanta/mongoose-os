namespace: http://mongoose-iot.com/fw
name: /v1/Config
methods:
  Get:
    doc: Get device config
    result:
      keep_as_json: true

  Set:
    doc: Set device config
    args:
      config:
        keep_as_json: true
  Save:
    doc: Save device config
