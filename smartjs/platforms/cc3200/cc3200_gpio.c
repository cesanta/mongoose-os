#include "smartjs/src/sj_gpio.h"

int sj_gpio_set_mode(int pin, enum gpio_mode mode, enum gpio_pull_type pull) {
  /* TODO(rojer): Impllement this. */
  return -1;
}

enum gpio_level sj_gpio_read(int pin) {
  /* TODO(rojer): Impllement this. */
  return GPIO_LEVEL_ERR;
}
